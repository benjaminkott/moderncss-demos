<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>{{ demo.name }}</title>
    {{ encore_entry_link_tags('inline') }}
    <style>
        {{ demo.styles|raw }}
    </style>
</head>
<body>
    {{ demo.content|raw }}
    {{ encore_entry_script_tags('inline') }}
</body>
